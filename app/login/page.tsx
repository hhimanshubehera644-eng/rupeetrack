"use client";

import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";

export default function LoginPage() {
  const router = useRouter();
  const [signup, setSignup] = useState(false);
  const [error, setError] = useState("");
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setError("");
    const data = new FormData(event.currentTarget);
    const response = await fetch("/api/auth", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: data.get("email"), password: data.get("password"), ...(signup ? { name: data.get("name") } : {}) }) });
    if (!response.ok) { setError((await response.json()).error ?? "Unable to continue."); return; }
    router.push("/");
    router.refresh();
  }
  return <main className="relative flex min-h-screen items-center justify-center overflow-hidden bg-slate-950 px-4 py-10 text-slate-900 sm:px-6"><div className="absolute inset-0 bg-[radial-gradient(circle_at_top_right,_rgba(20,184,166,0.28),_transparent_42%),radial-gradient(circle_at_bottom_left,_rgba(14,165,233,0.2),_transparent_38%)]" /><form onSubmit={submit} className="relative w-full max-w-md rounded-3xl border border-white/70 bg-white p-7 shadow-2xl sm:p-9"><div className="mb-8 flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-teal-500 text-xl font-bold text-white shadow-lg shadow-teal-500/25">₹</div><div><h1 className="text-2xl font-bold tracking-tight text-slate-950">Rupee<span className="text-teal-600">Track</span></h1><p className="text-xs font-medium uppercase tracking-wider text-slate-500">Personal finance, simplified</p></div></div><h2 className="text-2xl font-bold text-slate-950">{signup ? "Create your account" : "Welcome back"}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{signup ? "Start tracking your money privately and securely." : "Sign in to continue to your private finance dashboard."}</p><div className="mt-7 space-y-4">{signup && <div><label htmlFor="name" className="mb-1.5 block text-sm font-semibold text-slate-800">Your name</label><input id="name" required name="name" placeholder="Enter your name" className="w-full rounded-xl border-2 border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10" /></div>}<div><label htmlFor="email" className="mb-1.5 block text-sm font-semibold text-slate-800">Email address</label><input id="email" required type="email" name="email" placeholder="you@example.com" className="w-full rounded-xl border-2 border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10" /></div><div><label htmlFor="password" className="mb-1.5 block text-sm font-semibold text-slate-800">Password</label><input id="password" required minLength={8} type="password" name="password" placeholder="At least 8 characters" className="w-full rounded-xl border-2 border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10" /></div></div>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm font-medium text-rose-700">{error}</p>}<button className="mt-6 w-full rounded-xl bg-slate-950 py-3.5 font-semibold text-white shadow-lg transition hover:bg-slate-800 focus:outline-none focus:ring-4 focus:ring-slate-300">{signup ? "Create account" : "Sign in"}</button><div className="mt-6 border-t border-slate-200 pt-5 text-center"><p className="text-sm text-slate-600">{signup ? "Already have an account?" : "New to RupeeTrack?"}</p><button type="button" onClick={() => { setSignup(!signup); setError(""); }} className="mt-1 text-sm font-bold text-teal-700 underline decoration-teal-300 underline-offset-4 hover:text-teal-900">{signup ? "Sign in instead" : "Create a new account"}</button></div></form></main>;
}
